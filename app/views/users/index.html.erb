<h1>Users</h1>

<%= link_to "New user", new_user_path %>

<div id="users">
  <ul>
    <% @users.each do |user| %> <%# herb:disable all %>
      <li>
        <%= link_to user.username, edit_user_path(user.id) %>
      </li>
    <% end %>
  </ul>

</div>
